language: python
python:
  - '3.6' # I want to use Python 3.7, but travis ci not support it :( (2019-02-02)

install:
  - pip install pipenv
  - pipenv install --dev

script:
  - make test
  - make validate
  - make build
